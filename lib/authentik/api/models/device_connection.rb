=begin
This file is automatically generated by: https://openapi-generator.tech.
Any manual changes will be lost when the OpenAPI scheme changes.

=end

require 'date'
require 'time'

module Authentik::Api
  class DeviceConnection < ApiModelBase
    attr_accessor :device

    attr_accessor :connector

    attr_accessor :connector_obj

    attr_accessor :latest_snapshot

    # Attribute mapping from ruby-style variable name to JSON key.
    def self.attribute_map
      {
        :'device' => :'device',
        :'connector' => :'connector',
        :'connector_obj' => :'connector_obj',
        :'latest_snapshot' => :'latest_snapshot'
      }
    end

    # Returns attribute mapping this model knows about
    def self.acceptable_attribute_map
      attribute_map
    end

    # Returns all the JSON keys this model knows about
    def self.acceptable_attributes
      acceptable_attribute_map.values
    end

    # Attribute type mapping.
    def self.openapi_types
      {
        :'device' => :'String',
        :'connector' => :'String',
        :'connector_obj' => :'Connector',
        :'latest_snapshot' => :'DeviceFactSnapshot'
      }
    end

    # List of attributes with nullable: true
    def self.openapi_nullable
      Set.new([
        :'latest_snapshot'
      ])
    end

    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(attributes = {})
      if (!attributes.is_a?(Hash))
        fail ArgumentError, "The input argument (attributes) must be a hash in `Authentik::Api::DeviceConnection` initialize method"
      end

      # check to see if the attribute exists and convert string to symbol for hash key
      acceptable_attribute_map = self.class.acceptable_attribute_map
      attributes = attributes.each_with_object({}) { |(k, v), h|
        if (!acceptable_attribute_map.key?(k.to_sym))
          fail ArgumentError, "`#{k}` is not a valid attribute in `Authentik::Api::DeviceConnection`. Please check the name to make sure it's valid. List of attributes: " + acceptable_attribute_map.keys.inspect
        end
        h[k.to_sym] = v
      }

      if attributes.key?(:'device')
        self.device = attributes[:'device']
      else
        self.device = nil
      end

      if attributes.key?(:'connector')
        self.connector = attributes[:'connector']
      else
        self.connector = nil
      end

      if attributes.key?(:'connector_obj')
        self.connector_obj = attributes[:'connector_obj']
      else
        self.connector_obj = nil
      end

      if attributes.key?(:'latest_snapshot')
        self.latest_snapshot = attributes[:'latest_snapshot']
      else
        self.latest_snapshot = nil
      end
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties
      warn '[DEPRECATED] the `list_invalid_properties` method is obsolete'
      invalid_properties = Array.new
      if @device.nil?
        invalid_properties.push('invalid value for "device", device cannot be nil.')
      end

      if @connector.nil?
        invalid_properties.push('invalid value for "connector", connector cannot be nil.')
      end

      if @connector_obj.nil?
        invalid_properties.push('invalid value for "connector_obj", connector_obj cannot be nil.')
      end

      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid?
      warn '[DEPRECATED] the `valid?` method is obsolete'
      return false if @device.nil?
      return false if @connector.nil?
      return false if @connector_obj.nil?
      true
    end

    # Custom attribute writer method with validation
    # @param [Object] device Value to be assigned
    def device=(device)
      if device.nil?
        fail ArgumentError, 'device cannot be nil'
      end

      @device = device
    end

    # Custom attribute writer method with validation
    # @param [Object] connector Value to be assigned
    def connector=(connector)
      if connector.nil?
        fail ArgumentError, 'connector cannot be nil'
      end

      @connector = connector
    end

    # Custom attribute writer method with validation
    # @param [Object] connector_obj Value to be assigned
    def connector_obj=(connector_obj)
      if connector_obj.nil?
        fail ArgumentError, 'connector_obj cannot be nil'
      end

      @connector_obj = connector_obj
    end

    # Checks equality by comparing each attribute.
    # @param [Object] Object to be compared
    def ==(o)
      return true if self.equal?(o)
      self.class == o.class &&
          device == o.device &&
          connector == o.connector &&
          connector_obj == o.connector_obj &&
          latest_snapshot == o.latest_snapshot
    end

    # @see the `==` method
    # @param [Object] Object to be compared
    def eql?(o)
      self == o
    end

    # Calculates hash code according to all attributes.
    # @return [Integer] Hash code
    def hash
      [device, connector, connector_obj, latest_snapshot].hash
    end

    # Builds the object from hash
    # @param [Hash] attributes Model attributes in the form of hash
    # @return [Object] Returns the model itself
    def self.build_from_hash(attributes)
      return nil unless attributes.is_a?(Hash)
      attributes = attributes.transform_keys(&:to_sym)
      transformed_hash = {}
      openapi_types.each_pair do |key, type|
        if attributes.key?(attribute_map[key]) && attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = nil
        elsif type =~ /\AArray<(.*)>/i
          # check to ensure the input is an array given that the attribute
          # is documented as an array but the input is not
          if attributes[attribute_map[key]].is_a?(Array)
            transformed_hash["#{key}"] = attributes[attribute_map[key]].map { |v| _deserialize($1, v) }
          end
        elsif !attributes[attribute_map[key]].nil?
          transformed_hash["#{key}"] = _deserialize(type, attributes[attribute_map[key]])
        end
      end
      new(transformed_hash)
    end

    # Returns the object in the form of hash
    # @return [Hash] Returns the object in the form of hash
    def to_hash
      hash = {}
      self.class.attribute_map.each_pair do |attr, param|
        value = self.send(attr)
        if value.nil?
          is_nullable = self.class.openapi_nullable.include?(attr)
          next if !is_nullable || (is_nullable && !instance_variable_defined?(:"@#{attr}"))
        end

        hash[param] = _to_hash(value)
      end
      hash
    end

  end

end
