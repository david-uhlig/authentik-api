# -*- encoding: utf-8 -*-

=begin
This file is automatically generated by: https://openapi-generator.tech.
Any manual changes will be lost when the OpenAPI scheme changes.

=end

require_relative "lib/authentik/api/version"

Gem::Specification.new do |s|
  s.name        = "authentik-api"
  s.version     = Authentik::Api::VERSION
  s.platform    = Gem::Platform::RUBY
  s.authors     = ["David Uhlig"]
  s.email       = ["david.uhlig@gmail.com"]
  s.homepage    = "https://github.com/david-uhlig/authentik-api"
  s.summary     = "API client to talk to authentik&#39;s API."
  s.description = "API client to talk to authentik&#39;s API. Automatically generated with the OpenAPI Generator."
  s.license     = "MIT"
  s.required_ruby_version = ">= 3.3.0"
  s.metadata    = {}

  s.add_runtime_dependency 'typhoeus', '~> 1.0', '>= 1.0.1'
  s.add_runtime_dependency 'ostruct'
  s.add_runtime_dependency 'zeitwerk', '~> 2.6'

  s.add_development_dependency 'irb'
  s.add_development_dependency 'rspec', '~> 3.6', '>= 3.6.0'

  # Specify which files should be added to the gem when it is released.
  # The `git ls-files -z` loads the files in the RubyGem that have been added into git.
  gemspec = File.basename(__FILE__)
  s.files = IO.popen(%w[git ls-files -z], chdir: __dir__, err: IO::NULL) do |ls|
    ls.readlines("\x0", chomp: true).reject do |f|
      (f == gemspec) ||
        f.start_with?(*%w[.authentik/ .github/ .gitignore .openapi-generator/ .rspec .standard.yml Appraisals Gemfile VERSION bin/ docs/ gemfiles/ mise.toml spec/])
    end
  end
  s.test_files    = `find spec/*`.split("\n")
  s.executables   = []
  s.require_paths = ["lib"]
end
